# MVision 配置文件模板
# 复制此文件为 param.py 并根据实际环境修改参数

# ==================== PLC 服务器配置 ====================
PLC_SERVER_HOST = "0.0.0.0"  # 监听所有网络接口，生产环境建议改为具体 IP
PLC_SERVER_PORT = 2000       # TCP 端口

# ==================== 文件路径配置 ====================
# 标定文件路径 (相对于项目根目录)
CALIBRATION_FILE_PATH = "assets/calibration/affine_matrix.txt"

# 扫描区域配置文件 (相对于项目根目录)
SCAN_AREA_FILES = [
    "config/scan_areas/area_A.txt",
    "config/scan_areas/area_B.txt",
    "config/scan_areas/area_C.txt",
    "config/scan_areas/area_D.txt",
]

# ==================== 角度校正参数 ====================
# PLC 中角度行程不一样需要微调
# 角度旋转不准应该先调整方形工件无旋转角时候的偏移量，后调整此匹配行程
angle_deg_judge = 5.88

# ==================== HSV 颜色阈值配置 ====================
# 格式: (H_min, S_min, V_min), (H_max, S_max, V_max)
# H: 色相 (0-180), S: 饱和度 (0-255), V: 明度 (0-255)
hsv_range = {
    'yellow': ((5, 60, 13), (30, 255, 255)),
    'red1': ((0, 100, 20), (100, 255, 255)),      # 红色区间1
    'red2': ((156, 100, 20), (180, 255, 255)),    # 红色区间2 (红色跨越 0 度)
    'green': ((40, 50, 10), (90, 255, 255)),
    'blue': ((90, 85, 13), (160, 255, 255)),
}

# ==================== 调试备用配置 ====================
# 以下是备用的颜色阈值配置，可根据实际光照环境调整
# hsv_range_backup = {
#     'red1': ((0, 100, 20), (10, 255, 255)),
#     'red2': ((156, 100, 20), (180, 255, 255)),
#     'green': ((40, 50, 10), (90, 255, 255)),
#     'blue': ((90, 85, 13), (160, 255, 255)),
#     'yellow': ((10, 60, 13), (30, 255, 255)),
# }
